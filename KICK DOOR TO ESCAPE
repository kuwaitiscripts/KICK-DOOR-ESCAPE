local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "KUWAITI HUB V3.5 | ALL-IN-ONE 🚪🔥",
   LoadingTitle = "جاري تجميع القوى وفتح المسارات...",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false
})

-- متغيرات التحكم
_G.SmartTrain = false
_G.SmartWin = false
_G.SmartRebirth = false
_G.AutoEgg = false
_G.TimeBoost = false

--- [ التبويب الأول: الذكاء والإبادة ] ---
local MainTab = Window:CreateTab("الذكاء والفتح 🤖", 4483362458)

MainTab:CreateButton({
   Name = "فتح جميع الأبواب فوراً (Unlock All Doors)",
   Callback = function()
       pcall(function()
           -- تكرار الركل على جميع الأبواب في الماب
           for i = 1, 50 do -- تكرار لضمان الفتح
               game:GetService("ReplicatedStorage").Remote.Event.Game:FindFirstChild("[C-S]PlayerClick"):FireServer()
           end
           Rayfield:Notify({Title = "يوسف هب", Content = "تم إرسال أوامر فتح الأبواب!", Duration = 3})
       end)
   end,
})

MainTab:CreateToggle({
   Name = "تمرين ذكي (All Powers 1,2,3)",
   CurrentValue = false,
   Callback = function(Value)
       _G.SmartTrain = Value
       spawn(function()
           while _G.SmartTrain do
               pcall(function()
                   for _, folder in pairs(workspace.Scripts.Train:GetChildren()) do
                       for _, power in pairs(folder:GetChildren()) do
                           if power.Name:find("power") then
                               game:GetService("ReplicatedStorage").Remote.Event.Game:FindFirstChild("[C-S]PlayerTryTrain"):FireServer(power)
                           end
                       end
                   end
               end)
               task.wait(0.01)
           end
       end)
   end,
})

MainTab:CreateToggle({
   Name = "فوز تلقائي مصلح (Fixed Auto Win)",
   CurrentValue = false,
   Callback = function(Value)
       _G.SmartWin = Value
       spawn(function()
           while _G.SmartWin do
               pcall(function()
                   -- تفعيل وضع القتال لضمان عمل الفوز
                   game:GetService("ReplicatedStorage").Remote.Event.Setting:FindFirstChild("[C-S]PlayerTrySet"):FireServer("AutoFight", "1")
                   task.wait(0.1)
                   game:GetService("ReplicatedStorage").Remote.Event.Game:FindFirstChild("[C-S]PlayerWin"):FireServer("1")
               end)
               task.wait(0.5)
           end
       end)
   end,
})

--- [ التبويب الثاني: التجميع والريبيرث ] ---
local FarmTab = Window:CreateTab("التجميع والمال 💰", 4483362458)

FarmTab:CreateToggle({
   Name = "ريبيرث تلقائي (Auto Rebirth)",
   CurrentValue = false,
   Callback = function(Value)
       _G.SmartRebirth = Value
       spawn(function()
           while _G.SmartRebirth do
               game:GetService("ReplicatedStorage").Remote.Event.Eco:FindFirstChild("[C-S]PlayerTryRebirth"):FireServer()
               task.wait(2)
           end
       end)
   end,
})

FarmTab:CreateToggle({
   Name = "بوست وقت مجاني (Time Boost)",
   CurrentValue = false,
   Callback = function(Value)
       _G.TimeBoost = Value
       spawn(function()
           while _G.TimeBoost do
               game:GetService("ReplicatedStorage").Remote.Event.Boost:FindFirstChild("[C-S]PlayerTryAddTimeBoost"):FireServer()
               task.wait(10)
           end
       end)
   end,
})

--- [ التبويب الثالث: البتات والأدوات ] ---
local MiscTab = Window:CreateTab("أدوات إضافية 🛠️", 4483362458)

MiscTab:CreateToggle({
   Name = "فتح بيضة Egg3 تلقائياً",
   CurrentValue = false,
   Callback = function(Value)
       _G.AutoEgg = Value
       spawn(function()
           while _G.AutoEgg do
               local args = {[1] = "Egg3", [2] = 1}
               game:GetService("ReplicatedStorage").Remote.Function.Luck:FindFirstChild("[C-S]DoLuck"):InvokeServer(unpack(args))
               task.wait(0.5)
           end
       end)
   end,
})

MiscTab:CreateButton({
   Name = "تجهيز أفضل أداة (3_6)",
   Callback = function()
       game:GetService("ReplicatedStorage").Remote.Event.Game:FindFirstChild("[C-S]PlayerTryChangeTrainTool"):FireServer("3_6")
   end,
})

MiscTab:CreateButton({
   Name = "تشغيل SimpleSpy",
   Callback = function()
       loadstring(game:HttpGet("https://raw.githubusercontent.com/exxtremestuffs/SimpleSpySource/master/SimpleSpy.lua"))()
   end,
})

Rayfield:Notify({
   Title = "Kuwaiti Hub Ready",
   Content = "تم دمج جميع الأوامر في سكريبت واحد!",
   Duration = 5
})
