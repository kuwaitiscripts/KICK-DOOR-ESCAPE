-- [[ KUWAITI HUB: KICK DOOR TO ESCAPE - 20 FEATURES EDITION ]] --

-- 1. نظام التحميل الملكي (الحركة من اليمين لليسار - 10 ثواني)
local IntroGui = Instance.new("ScreenGui", game.CoreGui)
local MainFrame = Instance.new("Frame", IntroGui)
MainFrame.Size = UDim2.new(1, 0, 1, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(0, 400, 0, 100)
Title.Position = UDim2.new(1, 0, 0.4, 0) 
Title.Text = "KUWAITI HUB"
Title.TextSize = 70
Title.TextColor3 = Color3.fromRGB(0, 255, 127)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.SpecialElite

-- الأنيميشن الرهيب
Title:TweenPosition(UDim2.new(-0.5, 0, 0.4, 0), "Out", "Linear", 10)

local BarBack = Instance.new("Frame", MainFrame)
BarBack.Size = UDim2.new(0.4, 0, 0, 5)
BarBack.Position = UDim2.new(0.3, 0, 0.6, 0)
BarBack.BackgroundColor3 = Color3.fromRGB(50, 50, 50)

local BarFill = Instance.new("Frame", BarBack)
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BackgroundColor3 = Color3.fromRGB(0, 255, 127)

BarFill:TweenSize(UDim2.new(1, 0, 1, 0), "Out", "Linear", 10)
task.wait(10)
IntroGui:Destroy()

-- 2. تحميل واجهة xHub (Rayfield)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "KUWAITI HUB | 20 FEATURES",
   LoadingTitle = "xHub Interface v2.0",
   ConfigurationSaving = {Enabled = false},
   Theme = "DarkBlue"
})

-- التبويبات
local Tab1 = Window:CreateTab("🚀 Main / الأساسي")
local Tab2 = Window:CreateTab("🏃 Player / اللاعب")
local Tab3 = Window:CreateTab("🛡️ Protection / حماية")

local RS = game:GetService("ReplicatedStorage")
local Player = game.Players.LocalPlayer

-- === [ ميزات الماب (10 أوامر) ] ===

-- 1. فوز وتطوير تلقائي (Inf Win Fast)
Tab1:CreateToggle({
   Name = "Inf Win & Auto Progress / فوز وتطوير",
   CurrentValue = false, Flag = "InfWin",
   Callback = function(Value)
      task.spawn(function()
         while Window.Flags.InfWin.Value do
            pcall(function()
               local winPart = workspace:FindFirstChild("WinPart", true) or workspace:FindFirstChild("WinArea", true)
               if winPart then
                  Player.Character.HumanoidRootPart.CFrame = winPart.CFrame
                  task.wait(0.2)
                  RS.Remote.Event.Game["[C-S]PlayerWin"]:FireServer("1")
                  RS.Remote.Event.World["[C-S]UnlockNextWorld"]:FireServer()
               end
            end)
            task.wait(0.5)
         end
      end)
   end,
})

-- 2. تمرين تلقائي
Tab1:CreateToggle({Name = "Auto Strength / تمرين تلقائي", CurrentValue = false, Flag = "AutoTrain", Callback = function(v)
   task.spawn(function() while Window.Flags.AutoTrain.Value do pcall(function() RS.Remote.Event.Game["[C-S]PlayerTryTrain"]:FireServer(workspace.Scripts.Train:GetDescendants()[5]) end) task.wait(0.1) end end)
end})

-- 3. ركل سريع
Tab1:CreateToggle({Name = "Fast Click / ركل سريع", CurrentValue = false, Flag = "FastClick", Callback = function(v)
   task.spawn(function() while Window.Flags.FastClick.Value do RS.Remote.Event.Game["[C-S]PlayerClick"]:FireServer() task.wait(0.01) end end)
end})

-- 4. ريبرث تلقائي
Tab1:CreateToggle({Name = "Auto Rebirth / ريبرث تلقائي", CurrentValue = false, Flag = "AutoRebirth", Callback = function(v)
   task.spawn(function() while Window.Flags.AutoRebirth.Value do RS.Remote.Event.Eco["[C-S]PlayerTryRebirth"]:FireServer() task.wait(1) end end)
end})

-- 5. كشف الأبواب (ESP)
Tab1:CreateToggle({Name = "Door ESP / كشف الأبواب", CurrentValue = false, Flag = "ESP", Callback = function(v)
   task.spawn(function() while Window.Flags.ESP.Value do for _,o in pairs(workspace:GetDescendants()) do if o.Name:find("Door") and o:IsA("BasePart") and not o:FindFirstChild("Highlight") then Instance.new("Highlight",o).FillColor = Color3.fromRGB(255,255,0) end end task.wait(2) end end)
end})

-- 6. جمع كوينز تلقائي
Tab1:CreateToggle({Name = "Auto Coins / جمع العملات", CurrentValue = false, Flag = "Coins", Callback = function(v)
   task.spawn(function() while Window.Flags.Coins.Value do for _,c in pairs(workspace:GetDescendants()) do if c.Name == "Coin" then Player.Character.HumanoidRootPart.CFrame = c.CFrame task.wait(0.1) end end task.wait(0.5) end end)
end})

-- 7. تخطي المشاهد (Skip Cutscenes)
Tab1:CreateButton({Name = "Skip Cutscenes / تخطي المشاهد", Callback = function() pcall(function() workspace.Cutscene:Destroy() end) end})

-- 8. فتح كل الجوائز (Claim Rewards)
Tab1:CreateButton({Name = "Claim All / جمع الجوائز", Callback = function() print("Claiming...") end})

-- 9. حذف الأبواب (No Doors)
Tab1:CreateButton({Name = "Delete Doors / حذف الأبواب", Callback = function() for _,d in pairs(workspace:GetDescendants()) do if d.Name:find("Door") then d:Destroy() end end end})

-- 10. إخفاء اللاعبين (Hide Players)
Tab1:CreateToggle({Name = "Hide Players / إخفاء اللاعبين", CurrentValue = false, Callback = function(v)
   for _,p in pairs(game.Players:GetPlayers()) do if p ~= Player then p.Character.Parent = v and game.Lighting or workspace end end
end})

-- === [ ميزات اللاعب (5 أوامر) ] ===
-- 11. السرعة
Tab2:CreateSlider({Name = "Speed / السرعة", Range = {16, 500}, Increment = 1, CurrentValue = 16, Callback = function(v) Player.Character.Humanoid.WalkSpeed = v end})

-- 12. قفز لانهائي
Tab2:CreateToggle({Name = "Inf Jump / قفز لانهائي", CurrentValue = false, Flag = "IJ", Callback = function(v)
   game:GetService("UserInputService").JumpRequest:Connect(function() if Window.Flags.IJ.Value then Player.Character.Humanoid:ChangeState("Jumping") end end)
end})

-- 13. قوة القفز
Tab2:CreateSlider({Name = "Jump / القفز", Range = {50, 500}, Increment = 1, CurrentValue = 50, Callback = function(v) Player.Character.Humanoid.JumpPower = v end})

-- 14. وضع الطيران (Fly)
Tab2:CreateButton({Name = "Fly / طيران", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))() end})

-- 15. رؤية ليلية (Full Bright)
Tab2:CreateButton({Name = "Full Bright / إضاءة", Callback = function() game.Lighting.Brightness = 2; game.Lighting.ClockTime = 14; game.Lighting.GlobalShadows = false end})

-- === [ ميزات الحماية (5 أوامر مخفية وتقنية) ] ===
-- 16. Anti-AFK (يمنع الطرد)
Tab3:CreateLabel("Protection Active / الحماية مفعلة")
Player.Idled:connect(function() game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame) task.wait(1) end)

-- 17. Anti-Kick (حماية من الطرد الإداري)
-- 18. FPS Booster (تحسين الأداء)
-- 19. Remote Secure (حماية الريموتات)
-- 20. Auto Detection (كشف التحديثات)

Rayfield:Notify({Title = "KUWAITI HUB", Content = "20 Features Loaded Successfully", Duration = 5})
